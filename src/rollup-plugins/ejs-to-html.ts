/* import {createFilter} from 'rollup-pluginutils';
import * as ejs from "ejs";

export default function EjsToHtml() {
	return {
		name: 'ejs-to-html',
		generateBundle() {

			this.emitFile({ type: 'asset', fileName: 'package.json', source: JSON.stringify(pkg, null, 2) });
		}
	};
} */